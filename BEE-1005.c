//Problem is "Average 1"
#include <stdio.h>
int main() {
    float a, b, total;
    scanf("%f %f", &a, &b);
    total = ((a * 3.5) + (b * 7.5)) / 11;
    printf("MEDIA = %.5f\n", total);
    return 0;
}

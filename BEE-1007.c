//problem is "Difference"
#include <stdio.h>
int main() {
    int A,B,C,D,total;
    scanf("%d %d %d %d", &A, &B, &C, &D);
    total = (A*B - C*D);    
    printf("DIFERENCA = %d\n", total);
    return 0;
}
